{
  "name": "baustein-pfadivoels",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --port 8788",
    "db:init": "wrangler d1 execute baustein-db --local --file=schema.sql",
    "db:init:remote": "wrangler d1 execute baustein-db --remote --file=schema.sql",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
